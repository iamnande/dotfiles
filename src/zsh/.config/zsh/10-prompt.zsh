# fencing, fighting, torture
eval "$(starship init zsh)"
# TODO(nick): move this to macos, or use it, or remove it entirely
#eval "$(fnm env --use-on-cd --shell zsh)"

# we are but poor, lost, circus performers
nerdfetch
